"""
rule_engine.py — Generate world rules (physical, causal, systemic).

MVP behavior:
    - No real rule generation.
    - Returns empty rule list.
"""

def generate_rules(seed: dict, regions: list, entities: list):
    """
    Generate world rules.

    Returns
    -------
    list
        List of rules (MVP: empty).
    """
    return []
