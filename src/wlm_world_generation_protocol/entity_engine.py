"""
entity_engine.py — Generate entities (objects, agents, resources).

MVP behavior:
    - No real entity generation.
    - Returns empty entity list.
"""

def generate_entities(seed: dict, regions: list):
    """
    Generate world entities.

    Returns
    -------
    list
        List of entities (MVP: empty).
    """
    return []
